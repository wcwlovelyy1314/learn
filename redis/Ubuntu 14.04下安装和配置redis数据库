1.以root权限登录，切换到/usr目录下. 
2.接下来输入命令，apt-get install redis-server,
3.安装完成后，Redis服务器会自动启动，我们检查Redis服务器程序,执行ps -aux|grep redis
4.通过启动命令检查Redis服务器状态,执行：netstat -nlt|grep 6379
5.安装Redis服务器，会自动地一起安装Redis命令行客户端程序。在本机输入redis-cli命令就可以启动，
客户端程序访问Redis服务器。执行：redis-cli，
